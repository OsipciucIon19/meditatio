import styled from 'styled-components'

export const StyledFooter = styled.footer`
  margin-top: 3rem;
  background-color: #b4a696;
  
  .nav-item {
    margin: 1.5rem 0;
    color: #403B36FF;
    
    .nav-link {
      padding: 0;
      color: #403B36FF;
    } 
  }
`